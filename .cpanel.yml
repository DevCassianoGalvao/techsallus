---
deployment:
  tasks:
    # O repositorio agora tem a mesma estrutura flat que o site em producao
    # espera (nada de pasta public/ separada), entao nao ha nada pra copiar
    # ou reorganizar. "Update from Remote" ja deixa os arquivos no lugar
    # certo; este passo so existe pra satisfazer o requisito do cPanel de
    # ter um .cpanel.yml valido pra habilitar o botao "Deploy HEAD Commit".
    - /bin/true
